{
  "scripts": {
    "fmt": "prettier --write **/*.{js,json,html,tsx,ts}",
    "build:popup": "cd popup && yarn build",
    "build:background": "yarn tsc background/index.ts",
    "build": "yarn build:popup && yarn build:background"
  },
  "devDependencies": {
    "prettier": "2.8.4",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@types/chrome": "^0.0.217",
    "@types/micromatch": "^4.0.2",
    "micromatch": "^4.0.5",
    "tsc": "^2.0.4"
  }
}
